# Generated by starter-kit/scripts/export_participant_package.py.
# Standalone pipeline for the published participant repository.

stages:
  - test

starter-kit-unittest:
  stage: test
  image: python:3.11-slim
  before_script:
    - python --version
    - python -m pip install -r requirements.txt
  script:
    - python -m unittest discover -s tests
